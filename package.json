{
  "name": "stockanalysis",
  "version": "1.0.0",
  "description": "Japanese stock analysis dashboard",
  "private": true,
  "scripts": {
    "dev": "cd dashboard && npm run dev",
    "build": "cd dashboard && npm run build",
    "start": "cd dashboard && npm start"
  }
}
